head
  meta(charset="UTF-8")
  meta(name="viewport" content="width=device-width, initial-scale=1.0")
  meta(name="description" content=description || "Keith Barney — Design Systems Leader building scalable foundations for product teams.")
  title= title ? `${title} | Keith Barney` : "Keith Barney — Design Systems Leader"

  //- Open Graph
  meta(property="og:title" content=(title ? `${title} | Keith Barney` : "Keith Barney — Design Systems Leader"))
  meta(property="og:description" content=(description || "Design Systems Leader building scalable foundations for product teams."))
  meta(property="og:type" content="website")
  meta(property="og:url" content="https://keithbarney.design")
  meta(property="og:locale" content="en_US")

  //- Twitter Card
  meta(name="twitter:card" content="summary")
  meta(name="twitter:title" content=(title ? `${title} | Keith Barney` : "Keith Barney — Design Systems Leader"))
  meta(name="twitter:description" content=(description || "Design Systems Leader building scalable foundations for product teams."))

  //- Preload critical font
  link(rel="preload" href="fonts/TestAmericanGrotesk-Bold.otf" as="font" type="font/otf" crossorigin)

  //- Stylesheets
  link(rel="stylesheet" href="css/tokens.css")
  link(rel="stylesheet" href="css/main.css")
  link(rel="stylesheet" href="css/portfolio.css")
