//- ===========================================
//- MIXINS
//- Reusable Pug mixins for design tokens
//- ===========================================

//- Token grid header row (10-col grid: 2-2-2-2-2)
mixin tokenHeader(col1, col2, col3, col4, col5)
	span.token-header.col-span-2= col1
	span.token-header.col-span-2= col2
	span.token-header.col-span-2= col3
	span.token-header.col-span-2= col4
	span.token-header.col-span-2= col5

//- Section wrapper
mixin section(id, label)
	section.section(id=id)
		.section__content
			.section-label= label
			block

//- Subsection (no wrapper, just label and content)
mixin subsection(id, label)
	.subsection(id=id)
		.section-label= label
		block

//- Color token row (10-col grid: 2-2-2-2-2)
mixin colorRow(sectionTitle, groupTitle, swatchClass, name, value)
	span.color-token__section.col-span-2= sectionTitle
	span.color-token__group.col-span-2= groupTitle
	.color-token__visual.col-span-2
		.color-token__swatch(class=`color-swatch--${swatchClass}`)
	span.color-token__name.col-span-2
		span.token-copy(tabindex="0" role="button" aria-label=`Copy ${name}` onclick=`copyToken('${name}', this)` onkeydown=`handleCopyKeydown(event, '${name}', this)`)
			span.token-name= name
			span.copy-icon
				svg(width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true")
					rect(x="9" y="9" width="13" height="13" rx="2" ry="2")
					path(d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1")
	span.color-token__value.col-span-2= value

//- Semantic token row (10-col grid: 2-2-2-2-2)
mixin semanticRow(sectionTitle, groupTitle, swatchClass, alias, base)
	span.semantic-token__section.col-span-2= sectionTitle
	span.semantic-token__group.col-span-2= groupTitle
	.semantic-token__visual.col-span-2
		.semantic-token__swatch(class=`semantic-swatch--${swatchClass}`)
	span.semantic-token__alias.col-span-2
		span.token-copy(tabindex="0" role="button" aria-label=`Copy ${alias}` onclick=`copyToken('${alias}', this)` onkeydown=`handleCopyKeydown(event, '${alias}', this)`)
			span.token-name= alias
			span.copy-icon
				svg(width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true")
					rect(x="9" y="9" width="13" height="13" rx="2" ry="2")
					path(d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1")
	span.semantic-token__base.col-span-2= base

//- Typography size row (10-col grid: 2-2-2-2-2)
mixin typeRow(sectionTitle, groupTitle, sizeClass, name, value)
	span.type-size__section.col-span-2= sectionTitle
	span.type-size__group.col-span-2= groupTitle
	span.type-size__sample.col-span-2(class=`type-size__sample--${sizeClass}`) Aa
	span.type-size__name.col-span-2
		span.token-copy(tabindex="0" role="button" aria-label=`Copy ${name}` onclick=`copyToken('${name}', this)` onkeydown=`handleCopyKeydown(event, '${name}', this)`)
			span.token-name= name
			span.copy-icon
				svg(width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true")
					rect(x="9" y="9" width="13" height="13" rx="2" ry="2")
					path(d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1")
	span.type-size__value.col-span-2= value

//- Spacing token row (10-col grid: 2-2-2-2-2)
mixin spacingRow(sectionTitle, groupTitle, size, name, value)
	span.spacing-token__section.col-span-2= sectionTitle
	span.spacing-token__group.col-span-2= groupTitle
	.spacing-token__visual.col-span-2
		.spacing-token__bar(class=`spacing-token__bar--${size}`)
	span.spacing-token__name.col-span-2
		span.token-copy(tabindex="0" role="button" aria-label=`Copy ${name}` onclick=`copyToken('${name}', this)` onkeydown=`handleCopyKeydown(event, '${name}', this)`)
			span.token-name= name
			span.copy-icon
				svg(width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true")
					rect(x="9" y="9" width="13" height="13" rx="2" ry="2")
					path(d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1")
	span.spacing-token__value.col-span-2= value

//- Radius token row (10-col grid: 2-2-2-2-2)
mixin radiusRow(sectionTitle, groupTitle, size, name, value)
	span.radius-token__section.col-span-2= sectionTitle
	span.radius-token__group.col-span-2= groupTitle
	.radius-token__visual.col-span-2
		.radius-token__specimen(class=`radius-token__specimen--${size}`)
	span.radius-token__name.col-span-2
		span.token-copy(tabindex="0" role="button" aria-label=`Copy ${name}` onclick=`copyToken('${name}', this)` onkeydown=`handleCopyKeydown(event, '${name}', this)`)
			span.token-name= name
			span.copy-icon
				svg(width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true")
					rect(x="9" y="9" width="13" height="13" rx="2" ry="2")
					path(d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1")
	span.radius-token__value.col-span-2= value

//- Scale token row (10-col grid: 2-2-2-2-2) - for type scale and ui scale
mixin scaleRow(sectionTitle, groupTitle, size, name, value)
	span.scale-token__section.col-span-2= sectionTitle
	span.scale-token__group.col-span-2= groupTitle
	.scale-token__visual.col-span-2
		.scale-token__bar(style=`width: ${Math.min(size, 72)}px`)
	span.scale-token__name.col-span-2
		span.token-copy(tabindex="0" role="button" aria-label=`Copy ${name}` onclick=`copyToken('${name}', this)` onkeydown=`handleCopyKeydown(event, '${name}', this)`)
			span.token-name= name
			span.copy-icon
				svg(width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true")
					rect(x="9" y="9" width="13" height="13" rx="2" ry="2")
					path(d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1")
	span.scale-token__value.col-span-2= value
