{
  "$description": "Project-specific token overrides. Add values here to override the design system defaults.",
  "color": {
    "action": {
      "primary": {
        "$type": "color",
        "$value": { "hex": "#FC7F79" },
        "$description": "Override primary action color"
      }
    }
  },
  "font-family": {
    "mono": {
      "$type": "string",
      "$value": "Departure Mono",
      "$description": "Override monospace font"
    }
  }
}
